name: Datadog Static Analysis (PR Gates)

on:
  push:
    branches: [main, 'demo/**']
  pull_request:
    branches: [main]

# Runs Datadog Static Analysis (SAST + SCA + Secret Scanning + IaC) and uploads
# results to Datadog Code Analysis. PR Gates in Datadog then automatically creates
# a GitHub check on the PR that can be set as required to block bad merges.
#
# PR Gates config (Datadog UI):
#   Software Delivery > PR Gates > New Rule
#   Source: Static Code Analysis (SAST)
#   Condition: Introduces new Critical or High severity vulnerability
#   Scope: this repository
#
# Docs: https://docs.datadoghq.com/pr_gates/

jobs:
  datadog-static-analysis:
    name: Datadog Static Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write  # required for SARIF upload to GitHub Security tab
      pull-requests: write    # required for PR comment annotations

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      # ── SAST: Datadog Static Analysis ──────────────────────────────────────
      # v2.0.0 — supports Linux/X64 runners.
      # Note: dd_service, dd_env, fail_on_error were removed in v2+.
      # Service/env are inferred from git context; fail behaviour via diff_aware.
      - name: Run Datadog Static Analysis
        uses: DataDog/datadog-static-analyzer-github-action@2cc0e0621ea2863272b29d9aea230d9aad238086 # v2.0.0
        with:
          dd_api_key: ${{ secrets.DD_API_KEY }}
          dd_app_key: ${{ secrets.DD_APP_KEY }}
          dd_site: datadoghq.com
          diff_aware: true

      # ── SCA: Software Composition Analysis ─────────────────────────────────
      # v4.5.0 — composite action (no Docker build), generates SBOM and uploads to Datadog.
      # dd_service/dd_env not supported in v4+; service is inferred from git context.
      - name: Run Datadog SCA
        uses: DataDog/datadog-sca-github-action@37d973e751cff3de3d5dd4fc46160262688e1fa7 # v4.5.0
        with:
          dd_api_key: ${{ secrets.DD_API_KEY }}
          dd_app_key: ${{ secrets.DD_APP_KEY }}
          dd_site: datadoghq.com
